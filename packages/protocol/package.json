{
  "name": "@orbit/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Wire protocol schemas: generated from Rust (CDC, fills, sync schema) plus the client sync protocol",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./generated": "./src/generated/protocol.gen.ts",
    "./client": "./src/client-protocol.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
